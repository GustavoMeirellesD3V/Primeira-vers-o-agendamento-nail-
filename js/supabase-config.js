/* =========================================================
   NAIL STUDIO — supabase-config.js
   ---------------------------------------------------------
   Preencha com os dados do SEU projeto Supabase:
   Project Settings > API > Project URL / anon public key.

   IMPORTANTE: a "anon key" NÃO é um segredo — ela é feita pra
   ficar exposta no navegador (é assim que o Supabase funciona
   em qualquer site estático). A segurança de verdade vem das
   regras de Row Level Security (RLS) definidas em
   supabase/schema.sql, não de esconder esta chave.

   NUNCA coloque aqui a "service_role key" — essa sim é secreta
   e nunca deve aparecer em código que roda no navegador.
   ========================================================= */

const SUPABASE_URL = "https://vbxfgzcjfbequexrtzwa.supabase.co";
const SUPABASE_ANON_KEY = "sb_publishable_LZwkz-1gTXOmCMKb_ecLXw_gstmPYFL";
